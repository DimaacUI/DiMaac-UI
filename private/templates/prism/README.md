# Minimal · Prism

**Theo Anand** — dark portfolio with rainbow hairline and prismatic link hovers (CSS + WebGL wash).

## Files

- `index.html` — structure
- `styles.css` — dark theme, rainbow streak, hover colors
- `main.js` — clock + spectral WebGL background

## Run locally

```bash
npx serve .
```
