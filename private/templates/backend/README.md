# Terminal · Backend

**Jonas Beck** — terminal-inspired backend engineer portfolio with typed intro and API-style sections.

## Files

- `index.html` — structure
- `styles.css` — terminal chrome, panels, syntax colors
- `main.js` — typed hero animation, tab switching, copy helpers

## Run locally

```bash
npx serve .
```
